//Count vowels 
/* write a method that counts vowels(word) that takes in a string and returns the number of vowels in the word. Vowels are letters
a, e, i, o, u. */

function vowelCount(Str){
    let count = 0;
    arr = ['a', 'e', 'i', 'o', 'u'];
    for(let char in Str){
        if(char == ){
            count++
        }
    }
    console.log(count);
}

vowelCount('aeiollllu')
